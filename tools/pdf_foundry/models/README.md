Place local embedding model assets here.

Expected default files:

- `all-MiniLM-L6-v2.onnx`
- `all-MiniLM-L6-v2-tokenizer.json` (optional but recommended)

These files are intentionally not committed to the repository.
