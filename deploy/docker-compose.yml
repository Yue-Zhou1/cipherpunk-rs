services:
  audit-agent:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./audit-data:/data
    environment:
      - WORK_DIR=/data
      - AUDIT_WEB_EVENTS_POLL_MS=2000
